//
//  GCStatsCluster.m
//  RZUtils
//
//  Created by Brice Rosenzweig on 18/10/2020.
//  Copyright © 2020 Brice Rosenzweig. All rights reserved.
//

#import "GCStatsCluster.h"

@implementation GCStatsCluster

-(void)kmean{
    /*
    size_t n_points; // number of points
    size_t n_dimensions; // dimension of point (2d -> 2, etc)
    size_t n_clusters; // number of cluster

    double points[n_dimensions*n_points]; // points[ i_point * d + i_dimension ]
    size_t cluster_allocation[n_points]; // cluster_allocation[ i_point * k + i_cluster ] =  0 or 1 (allocated)
    double cluster_center[n_clusters*n_dimensions*n_points]; // cluster_center[ i_point * (k*d) + (i_cluster * d) + i_dimension ]
    
    for( size_t i_point = 0; i_point < n_points; i_point++ ){
        // for each point:
        
        
    }

    //for( k in 0..n)
      //dist_k = (p_d - c_k)' * (p_d - c_k);
      //  c = k if dist_k < curr_k;
    //curr_k = dist_k;
      

*/

}

@end
